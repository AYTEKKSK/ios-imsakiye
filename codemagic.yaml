workflows:
  ios-workflow:
    name: iOS Workflow
    environment:
      xcode: latest

    scripts:
      - name: Build ios
        script: |
          xcodebuild build -workspace src/imsakiye.xcworkspace -scheme imsakiye

    artifacts:
      - build/ios/**/*.ipa

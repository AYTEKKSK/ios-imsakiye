workflows:
  ios-workflow:
    name: iOS Workflow

    environment:
      xcode: latest

    scripts:
      - name: Install CocoaPods
        script: |
          cd src
          pod install

      - name: Build iOS
        script: |
          cd src
          xcodebuild build \
          -workspace imsakiye.xcworkspace \
          -scheme imsakiye \
          -sdk iphoneos \
          -configuration Release

    artifacts:
      - src/build/**/*.ipa
